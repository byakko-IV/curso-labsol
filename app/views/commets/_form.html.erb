<%= form_for([@post, @comment]) do |f| %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.hidden_field :post_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
